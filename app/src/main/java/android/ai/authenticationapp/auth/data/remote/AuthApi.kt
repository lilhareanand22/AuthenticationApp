package android.ai.authenticationapp.auth.data.remote

/**
 * Data layer: Implements repositories, data sources, and network APIs to fetch and persist data.
 */
interface AuthApi {
    // TODO: Define Retrofit API endpoints
}
