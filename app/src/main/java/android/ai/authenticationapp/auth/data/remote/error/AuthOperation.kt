package android.ai.authenticationapp.auth.data.remote.error

/**
 * Data layer: Represents the specific authentication operation being performed.
 */
enum class AuthOperation {
    LOGIN,
    GET_CURRENT_USER,
    REFRESH
}
